{
  "name": "template",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start:server": "npm start -w apps/server",
    "start:client": "npm start -w apps/client",
    "push": "npm run push -w packages/db",
    "generate": "npm run generate -w packages/db"
  }
}
